import React from "react";

export default function LookingForWork() {
  return (
    <div
      style={{
        backgroundColor: "blue",
        height: "10rem",
        width: "10rem",
        borderRadius: "50%",
        color: "#EEE",
        display: "flex",
        flexDirection: "center",
        alignItems: "center",
        padding: "1rem",
      }}
    >
      <p style={{ textAlign: "center" }}>Currently Looking For Work</p>
    </div>
  );
}
